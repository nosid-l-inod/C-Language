#include <stdio.h>
#define constante 20 // Outra forma de declarar uma constante

int main() {
    // Constant = fixed value -> Cant be altered
    // Const Uppercase

    const float PI = 3.14159;

    printf("%f", PI);
    
    return 0;
}