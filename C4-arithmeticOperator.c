// Author: doni
// Date: 24-11-2024
// Description: Variables in C language

#include <stdio.h>

int main() {

    int x = 5;
    int y = 2;

    // Sum
    int sum = x + y;

    // Subtraction
    int subtraction = x - y;

    // Multiplication
    int multiplication = x * y;

    // Division
    // When dividing, convert convert it to float
    int division = (float) x / y;
    
    // remainder
    // Returns the remainder of divisions
    int remainder = x % y;
    
    return 0;
}